<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileProject"
        tools:targetApi="31">
        <activity
            android:name=".ui.home.activity_about_us"
            android:exported="false" />
        <!-- Welcome Screen (หน้าจอต้อนรับ) -->
        <activity
            android:name=".welcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Login Screen (ไม่ใช่หน้าแรก) -->
        <activity
            android:name=".Login"
            android:exported="true"
            android:label="@string/app_name"></activity> <!-- Loading Screen -->
        <activity
            android:name=".Loading"
            android:exported="false" /> <!-- Main Screen (หน้าหลักหลังจาก Login) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" /> <!-- Font Preload -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>